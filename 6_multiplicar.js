/* 3. Multiplicar por 2
Usa el ciclo para mostrar los resultados de multiplicar el número actual por 2, cada vez:

Empieza desde 1 hasta 5.
Multiplica el número por 2 y muestra el resultado.
Esto ayuda a ver cómo el valor de la variable cambia en cada iteración, como si fueran pequeños pasos. */

function multiplicar (num){  

    for (let i = 1; i <= 11; i++){
        console.log(i * num);
    }
    return num
}
multiplicar(3)
 

