/* Comprueba si dos números son iguales. */

function comprobacionDeNumeros(a,b) {
    if (a === b) {
        console.log("es el mismo numero");
    } else if(a !== b){
        console.log("son diferentes numeros");
    }
}
comprobacionDeNumeros(1,2)