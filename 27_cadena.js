/* Comprueba si la longitud de una cadena es mayor que 5. */
function largoCadena(cadenaTexto) {
    if (cadenaTexto.length > 5) {
        console.log("es muy largo el texto");
    } else {
        console.log("demacioado corto"); 
    }
}
largoCadena("baila")