/* 1. Suma de dos números: Escribe una función que reciba dos números como argumentos y devuelva su suma. */

function suma(num1, num2) {
    let resultado = num1 + num2
    return resultado
} 
console.log(suma(23,34));

